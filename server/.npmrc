# Делает поле engines из package.json настоящим ограничением, а не пометкой.
# Без этой строки npm молча ставит пакеты на неподходящей версии Node,
# и о несовместимости узнаёшь уже при запуске.
engine-strict=true
