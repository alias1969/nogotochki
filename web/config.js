/**
 * Единственное место, где записан адрес API.
 *
 * Адрес целиком, со схемой и портом: страница раздаётся отдельно от API,
 * и для браузера это другой origin. Чтобы запросы отсюда проходили,
 * этот же адрес должен стоять в переменной WEB_ORIGINS на сервере —
 * иначе браузер не отдаст странице даже успешный ответ.
 *
 * Пустая строка означает «тот же origin, что и страница»: так пишут,
 * когда статику и API отдаёт один домен (nginx на проде), — тогда
 * запросы уходят на /api/... и разрешение CORS не нужно вовсе.
 */
export const API_BASE = 'http://localhost:3000';

/** Сколько карточек-заглушек показывать, пока список едет с сервера. */
export const SKELETON_COUNT = { services: 3, masters: 4 };
